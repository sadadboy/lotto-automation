# 로또 자동구매 시스템 환경변수 설정
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요

# ==============================================
# 🔐 필수 인증정보 (반드시 설정해야 함)
# ==============================================
LOTTO_USER_ID=your_lotto_user_id
LOTTO_PASSWORD=your_lotto_password

# ==============================================
# 🎯 구매 설정 (선택사항 - 기본값 사용 가능)
# ==============================================
# 게임 구매 개수 (기본값: 5)
LOTTO_PURCHASE_COUNT=5

# 자동충전 사용 여부 (기본값: true)
LOTTO_AUTO_RECHARGE=true

# 충전 금액 원 단위 (기본값: 10000)
LOTTO_RECHARGE_AMOUNT=10000

# 최소 잔액 원 단위 (기본값: 5000)
LOTTO_MIN_BALANCE=5000

# ==============================================
# 🖥️ 시스템 옵션
# ==============================================
# 헤드리스 모드 (기본값: true)
LOTTO_HEADLESS=true

# 스크린샷 저장 (기본값: true)
LOTTO_SCREENSHOT=true

# 디버그 모드 (기본값: false)
LOTTO_DEBUG=false

# ==============================================
# 📅 스케줄 설정 (크론 형식)
# ==============================================
# 실행 스케줄 - 매주 월요일, 목요일 14시 (기본값: 0 14 * * 1,4)
CRON_SCHEDULE=0 14 * * 1,4

# ==============================================
# 🔔 Discord 알림 (선택사항)
# ==============================================
# Discord 웹훅 URL (알림을 받으려면 설정)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_URL

# ==============================================
# 🌐 모니터링 (선택사항)
# ==============================================
# 모니터링 웹서버 포트 (기본값: 8080)
MONITOR_PORT=8080

# ==============================================
# 💡 사용 예시
# ==============================================
# 즉시 실행:
#   docker-compose -f docker-compose.automated.yml --profile immediate up --build
#
# 스케줄러 실행:
#   docker-compose -f docker-compose.automated.yml --profile scheduler up -d --build
#
# 모니터링 포함:
#   docker-compose -f docker-compose.automated.yml --profile scheduler --profile monitor up -d --build
#
# 모든 서비스:
#   docker-compose -f docker-compose.automated.yml --profile immediate --profile scheduler --profile monitor up -d --build
