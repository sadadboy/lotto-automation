# 로또 자동구매 환경변수 설정 파일
# 사용법: cp .env.example .env 후 실제 값으로 수정

# 마스터 패스워드 (인증정보 암호화용)
LOTTO_MASTER_PASSWORD=your_master_password_here

# 시간대 (옵션)
TZ=Asia/Seoul

# 디버그 모드 (옵션)
DEBUG=false

# Discord 웹훅 URL (옵션 - config 파일에서도 설정 가능)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Chrome 옵션 (자동화 환경용)
CHROME_NO_SANDBOX=true
CHROME_DISABLE_GPU=true
CHROME_DISABLE_DEV_SHM=true

# 로그 레벨 (옵션)
LOG_LEVEL=INFO
